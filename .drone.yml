---
image: bradrydzewski/python:2.7
script:
  - python -V
publish:
  swift:
    username: {{username}}
    password: {{api_key}}
    auth_url: {{auth_url}}
    region: {{region}}
    container: drone
    source: /var/cache/drone/src/github.com/sivel/drone-test/README.md
